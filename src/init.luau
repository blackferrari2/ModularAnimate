local Main = require(script.Main)
local Config = require(script.Config)
local Poses = require(script.PoseController).Poses

---------------

return {
    Main = Main,
    Config = Config,
    Poses = Poses,
}